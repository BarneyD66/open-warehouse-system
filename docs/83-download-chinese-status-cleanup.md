# 下载内容中文化补丁

## 本次修正

- 客户出库订单 CSV 的订单状态改为中文，不再直接导出 `picking`、`shipped` 等系统值。
- 客户出库订单 CSV 的 SKU 明细数量连接符统一为更清晰的 `×`。
- SKU 档案 CSV 的状态改为中文，不再直接导出 `active`、`paused`、`archived`。

## 后续原则

- 中文模式下，客户可下载内容以中文表头、中文状态和中文示例为主。
- 英文系统字段只保留在导入兼容别名中，不作为默认导出展示。
