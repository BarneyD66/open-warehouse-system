# 生产集成配置向导中文清理与导出

## 本次目标

对标马帮的上线前配置检查能力，把承运商 API、平台订单 API、对象存储、外部通知、定时报表和生产安全的配置缺口变成员工能看懂、能导出的清单。

## 已完成

- 重写生产集成体检模块，恢复中文名称、摘要、环境变量说明和下一步动作。
- 保留原有 readiness 输出结构：评分、ready/partial/blocked 汇总、分组、环境变量、下一步动作。
- 重写平台网关文案，清理 Shopify、eBay、通用平台 API 的拉单、取消订单、发货回传错误提示和同步备注。
- 清理集成联调按钮文案。
- 运营后台“生产集成配置向导”面板恢复中文，并新增“导出配置清单”入口。
- `/api/ops/integrations/readiness?format=csv` 支持导出生产集成配置清单，覆盖分组、集成项、状态、模式、环境变量、是否已配置、是否必填和下一步动作。
- 复核 Webhook 回调台账，承运商轨迹/POD 与平台取消订单回调记录保持中文展示。

## 验证

- `npm run lint` 已通过。
- 待随本轮整体执行 `npm run build`。
