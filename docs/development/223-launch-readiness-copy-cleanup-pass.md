# 223 上线体检中文化清理

## 本轮目标

- 清理上线体检核心逻辑中的历史乱码，保证运营后台和健康检查接口可直接阅读。
- 保留现有上线判断逻辑，不引入新的业务范围，避免影响稳定性。

## 已完成

- 重写 `launchReadiness` 的检查项文案、负责人、风险说明和处理建议。
- 覆盖数据库持久化、文件持久化、文件安全、三端域名、员工白名单、演示登录、客户账号、仓储物流闭环、账单闭环、库位容量、承运商闭环、平台 API 同步、备份恢复等上线检查。
- 修复承运商能力检查中的乱码能力项，包括面单购买、轨迹自动回传、派送失败处理、签收证明和物流赔付。
- 保持上线评分、最差状态计算和 `/api/health` 依赖结构不变。

## 验证

- 乱码关键词扫描：通过。
- `npm run lint`：通过。
- `npm run build`：通过。

## 下一步

- 继续做生产集成探测、真实接口配置引导和上线守门板的体验细化。
- 持续扫描导出、报表和后台面板里的历史英文/乱码文案。
