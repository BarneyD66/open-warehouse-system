# 仓库后台承接补资料交付记录

## 市场团队交接

市场团队把中文客户侧流程继续交给产品经理：客户在门户补交资料后，仓库后台必须能立即看到，否则流程会停在客服聊天里，无法形成可执行的 WMS 队列。

## 产品经理拆解

- `/ops` 需要展示真实入库预报队列。
- 运营指标需要区分合作询盘、入库预报、待补资料、待补追踪号。
- 每个 ASN 卡片需要显示追踪号、附件数量、最近更新时间和补充说明。
- 状态标签按资料完整度变化：待补资料、待补追踪号、待仓库审核。

## 已完成

- `/ops` 接入补资料后的真实字段。
- 入库预报卡片新增：
  - 追踪号
  - 附件数量
  - 最近更新时间
  - 附件名称列表
  - 客户补充说明
- 运营队列新增待处理事项汇总。
- 桌面端和 390px 移动端均验证无横向滚动。

## 验证

- `npm run lint` 通过。
- `npm run build` 通过。
- Playwright 验证 `/ops` 桌面端和移动端均能看到 `DHL-UPLOAD-9901`。
- Playwright 验证 `/ops` 桌面端和移动端均能看到 `packing-list-test.txt`。
